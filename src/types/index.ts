export type ProjectData = {
	title: string;
	description: string;
    imageUrl: string;
    webLink: string
    appLink: string
};

export type SkillsData = {
    title: string
    description: string
    icon: any
}

export type ContactData = {
	title: string;
	description: string;
    icon: any;
    linkUrl: string
};
